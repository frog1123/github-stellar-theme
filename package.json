{
  "name": "github-stellar-theme",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run ts && npm run sass && npm run manifest && npm run icons",
    "ts:w": "tsc -w",
    "sass:w": "sass --watch --style=compressed --no-source-map src/styles:dist/styles",
    "ts": "tsc",
    "sass": "sass --style=compressed --no-source-map src/styles:dist/styles",
    "manifest": "node scripts/manifest.js",
    "icons": "node scripts/icons.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.55.0",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "chalk": "4.1.2"
  }
}
